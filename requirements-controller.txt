# Controller runtime plus Feishu adapter dependencies baked into the derived image.
# Keep these versions aligned with runtime verification and upgrade compatibility CI.
pydantic==2.13.4
PyYAML==6.0.3
lark-oapi==1.6.8
qrcode==7.4.2
requests-toolbelt==1.0.0
