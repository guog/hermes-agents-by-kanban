name: task-reviewer
version: 0.1.0
description: Analyzes SPEC, PLAN and TASKS consistency without modifying artifacts
hermes_requires: ">=0.18.2"
author: Hermes MES Team
license: Proprietary
env_requires:
  - name: GITLAB_HOST
    description: Private GitLab hostname
    required: true
  - name: GITLAB_TOKEN
    description: Task reviewer token with MR comment permission
    required: true
