name: tasker
version: 0.2.0
description: Writes one-to-one TASKS files and a stable executable task DAG
hermes_requires: ">=0.19.0"
author: Hermes MES Team
license: Proprietary
env_requires:
  - name: GITLAB_HOST
    description: Private GitLab hostname
    required: true
  - name: GITLAB_TOKEN
    description: Developer token with shared branch and MR permission
    required: true
