name: coder
version: 0.2.0
description: Implements the complete approved TASKS set in one shared PRD delivery MR
hermes_requires: ">=0.19.0"
author: Hermes MES Team
license: Proprietary
env_requires:
  - name: GITLAB_HOST
    description: Private GitLab hostname
    required: true
  - name: GITLAB_TOKEN
    description: Developer token with shared branch and MR permission
    required: true
