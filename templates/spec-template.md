<!--
参考：github/spec-kit templates/spec-template.md
基线：4d3a4281bc63bd2af9f2515bb1036fc38da1294e
适配：Hermes 单 PRD、共享分支、唯一交付 MR、每个 key 一份 SPEC。
删除本注释中的说明性占位内容后再提交正式工件。
-->

# 功能规格：[功能名称]

**SPEC Key**：`[lowercase-kebab-case]`

**来源 PRD**：`[PRD 路径与固定 commit/blob 链接]`

**共享分支**：`[交付分支]`

**创建日期**：`[YYYY-MM-DD]`

**状态**：草稿

**输入**：`[本 SPEC 覆盖的 PRD 范围与原始描述]`

## 冻结输入与阶段内决策（必填）

- PRD 固定 blob：`[40-char-git-blob-sha]`
- 仓库基线：`[repository_base_sha；run 启动时默认分支 SHA]`
- 上游不可变规则：本阶段不得修改 PRD；遗漏、歧义或矛盾依次按安全、验收条件、
  具体规则、仓库契约、兼容性和最小可逆范围解释。
- 阶段内关键决策：`[决策、依据、影响与可逆方式；无则写“无”]`
- 未解决问题与残余风险：`[finalization 时必须逐项列出；无则写“无”]`

## 现有 MES 基线与需求增量（必填）

本功能是对现有企业 MES 的定制，不是从零设计。这里只描述可观察现状和业务增量，
不提前指定技术实现。

| 现有能力/流程 | 仓库证据（代码、文档或测试路径） | 当前可观察行为 | PRD 要求的变化 | 变更类型 |
| --- | --- | --- | --- | --- |
| `[能力]` | `[repository_base_sha:path]` | `[现状]` | `[新增或修改后的结果]` | 扩展/修改/扩展并修改 |

- 计划复用的现有能力：`[业务能力、规则、页面流程或契约；无同类能力时写现有承载框架]`
- 必须保持的现有行为与兼容边界：`[未被 PRD 改变的行为]`
- 明确不新建的平行能力：`[避免重复造轮子的范围]`

## 用户场景与测试（必填）

<!--
按业务价值从高到低排列用户故事（P1、P2、P3……）。
每个故事必须能够独立实现、独立测试，并在只交付该故事时产生可演示的价值。
本文件只描述 WHAT/WHY 与可观察结果，不写技术选型、模块设计或实现步骤。
-->

### 用户故事 1：[简短标题]（优先级：P1）

[用业务语言描述用户旅程、参与者、目标和获得的价值。]

**优先级理由**：[说明价值及为何属于该优先级。]

**独立验证**：[说明仅实现本故事时，如何通过具体操作验证其业务价值。]

**验收场景**：

1. **假如** [初始状态]，**当** [用户动作或事件]，**那么** [可观察结果]。
2. **假如** [初始状态]，**当** [用户动作或事件]，**那么** [可观察结果]。

---

### 用户故事 2：[简短标题]（优先级：P2）

[用业务语言描述用户旅程。]

**优先级理由**：[说明价值及优先级理由。]

**独立验证**：[说明独立验证方式。]

**验收场景**：

1. **假如** [初始状态]，**当** [动作或事件]，**那么** [可观察结果]。

---

[按同一结构补充更多用户故事。]

### 边界与异常场景

- 当 [边界条件] 时，系统应 [可观察行为]。
- 当 [错误或依赖不可用] 时，系统应 [降级、恢复或提示行为]。
- 当 [重复、并发或乱序输入] 时，系统应 [幂等性或冲突处理行为]。
- 明确不在本 SPEC 范围内的场景：[范围外内容及归属]。

## 需求（必填）

### 功能需求

- **FR-001**：系统必须 [具体、无歧义且可测试的能力]。
- **FR-002**：系统必须 [输入、校验或状态转换要求]。
- **FR-003**：用户必须能够 [关键交互]。
- **FR-004**：系统必须 [失败、恢复或兼容性行为]。
- **FR-005**：系统必须 [审计、安全或可追溯要求]。

### 关键实体（涉及数据时填写）

- **[实体 1]**：[业务含义、关键属性、约束；不写数据库或类的实现方案]。
- **[实体 2]**：[业务含义及其与其他实体的关系]。

## 成功标准（必填）

<!--
成功标准必须可量化、技术无关，并能从用户或业务结果验证。
不要使用框架名、数据库名或内部实现指标替代业务结果。
-->

- **SC-001**：[例如：目标用户在 2 分钟内完成主要流程]。
- **SC-002**：[例如：在约定规模下，95% 的请求在目标时间内得到可用结果]。
- **SC-003**：[例如：90% 的首次使用者无需帮助即可完成主要任务]。
- **SC-004**：[例如：与该问题相关的人工处理量降低 50%]。

## 假设

<!--
记录需求未明确但可依据合理默认值作出的假设。关键决策还要按交付流程写入 MR。
-->

- [目标用户、运行环境或数据前提]。
- [复用现有能力、外部依赖或权限前提]。
- [版本范围、兼容性或明确的范围边界]。

## PRD 覆盖与追溯（必填）

| PRD 条目/段落 | 本 SPEC 用户故事 | 功能需求 | 验收场景/成功标准 |
| --- | --- | --- | --- |
| `[PRD 引用]` | US1 | FR-001、FR-002 | US1-场景1、SC-001 |
| `[PRD 引用]` | US2 | FR-003 | US2-场景1、SC-002 |
